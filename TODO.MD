Wenn ich auf das Tray icon klicke soll es so ähnlich wie bei Onedrive sein, dass ich schnell einen timer starten kann mit den favoriten und den letzten timer welcher ich benutzt hatte ebenfalls falls n timer läuft diesen anzeigenb und mit stop button.

Das Programm startet nicht selbst bei pc neustart